<%= form_with(model: bookmarked_product, local: true) do |form| %>
  <% if bookmarked_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmarked_product.errors.count, "error") %> prohibited this bookmarked_product from being saved:</h2>

      <ul>
        <% bookmarked_product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
